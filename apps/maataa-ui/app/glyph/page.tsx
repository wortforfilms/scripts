import { ToolPage } from "../../components/tools/ToolPage";

export default function GlyphPage() {
  return <ToolPage featureKey="glyphInspector" title="Glyph Inspector" description="Review verified glyph rendering and verification-required placeholders." />;
}
