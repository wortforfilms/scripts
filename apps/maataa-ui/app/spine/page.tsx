import { ToolPage } from "../../components/tools/ToolPage";

export default function SpinePage() {
  return <ToolPage featureKey="spineEvents" title="Maataa Spine" description="Stream audit events and runtime activity without sensitive payment data." />;
}
