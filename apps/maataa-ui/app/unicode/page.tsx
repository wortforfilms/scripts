import { ToolPage } from "../../components/tools/ToolPage";

export default function UnicodePage() {
  return <ToolPage featureKey="unicodeVisualizer" title="Unicode Visualizer" description="Inspect Unicode-derived script records and safe partial metadata." />;
}
